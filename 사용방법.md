# 사용방법

## 시작 (외래 시작 시 1회)

1. **LM Studio** 실행 → 모델 `gemma-3-12b-it-qat` 로드 (Context 3072, Auto-Evict ON)
2. 터미널 1 — 백엔드:
   ```bash
   uv run uvicorn backend.main:app --port 8080
   ```
3. 터미널 2 — 프론트엔드:
   ```bash
   cd frontend && pnpm dev
   ```
4. 브라우저: http://127.0.0.1:5173

## 환자당 사용

1. **녹음 버튼** 클릭 → 발화 → 정지
2. STT 텍스트 자동 표시 (10–15초)
3. SOAP 4 섹션 자동 추가 (30–40초 후)
4. textarea에서 검토/편집 → **전체 복사** 버튼 → EMR 붙여넣기

전체 복사 시점에 편집 diff가 `logs/edits.jsonl`에 자동 저장됨 (학습용, 마스킹 후).

## 종료

각 터미널에서 `Ctrl+C`. LM Studio도 종료.

---

## 자주 마주칠 상황

- **SOAP가 비어 나옴**: 의도된 보수적 동작. textarea에서 직접 입력.
- **응답 매우 느림**: LM Studio Auto-Evict 후 첫 요청 — 15-20초 추가. 두 번째부터 정상.
- **녹음 안 됨**: 브라우저 마이크 권한 확인 (127.0.0.1:5173).
