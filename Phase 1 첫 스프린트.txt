Phase 1 첫 스프린트: STT 백엔드 모듈화                                                                                                                                                                                                          Context                                                                                                                 /Users/chlee/Developer/Claude_workplace_macmini/Voice_SOAP/에는 이미 5개 Phase에 걸친 마스터 플랜(plan.md)과 검증된 PoC 자산(whisper_test.py, medical_hints.txt, recording.wav)이 있다. PoC에서 mlx-whisper large-v3 + 의학 힌트 조합으로 간장학 용어 인식 정확도 ~85%, 캐시 후 RTF 0.10x를 검증했다. 이번 스프린트는 마스터 플랜의 Phase 1 (백엔드 코어) 중 첫 절반만 구현한다. 즉: - 단일 스크립트(whisper_test.py)에 흩어진 PoC 로직을 모듈화된 FastAPI 백엔드로 옮긴다 - POST /transcribe 엔드포인트로 wav → 한국어 텍스트(의학 힌트 적용) 변환을 HTTP API로 제공한다 - LLM SOAP 구조화(/soap), 후처리 사전, LaunchAgent 등록은 다음 스프린트로 미룬다 ― 사용자가 "STT 모듈 + /transcribe만"으로 명시 선택 목표는 1~3일 안에 curl로 recording.wav → 한국어 텍스트 JSON 응답이 나오는 검증 가능한 기반을 만드는 것이다. LM Studio Gemma 4 MoE 통합은 이 기반 위에서 이어진다. 산출물 정의 성공 기준: 1. uv run uvicorn backend.main:app로 서버가 뜬다 2. curl -X POST http://localhost:8080/transcribe -F "audio=@recording.wav"이 SOAP 직전 단계의 한국어 텍스트 + 메타데이터(처리 시간, RTF, 신뢰도 낮은 구간)를 JSON으로 반환한다 3. /docs에서 Swagger UI로 직접 테스트 가능 4. 같은 wav를 whisper_test.py로 돌렸을 때와 결과 텍스트가 동일하다 (회귀 검증) 디렉토리 구조 (이번 스프린트 한정) plan.md 8번 섹션의 전체 구조 중, 이번에 만들 것만 표시: Voice_SOAP/ ├── plan.md                          # 기존 (마스터) ├── pyproject.toml                   # 신규 (uv) ├── .python-version                  # 신규 (3.11) ├── .gitignore                       # 신규 ├── README.md                        # 신규 (간단 실행법만) │ ├── backend/ │   ├── __init__.py │   ├── main.py                      # FastAPI 앱 (CORS, /healthz, 라우터 등록) │   ├── config.py                    # 설정 (모델 경로, 힌트 파일, 포트) │   ├── api/ │   │   ├── __init__.py │   │   └── transcribe.py            # POST /transcribe │   └── stt/ │       ├── __init__.py │       ├── whisper_engine.py        # transcribe() 래퍼 ― PoC에서 이식 │       └── hints_loader.py          # load_hints() ― PoC에서 이식 │ ├── hints/ │   └── medical_hints.txt            # PoC에서 이동 (심볼릭 링크 X, 복사) │ └── tests/     ├── __init__.py     ├── test_hints_loader.py     └── fixtures/         └── recording.wav            # PoC에서 이동/복사 미적용 (다음 스프린트): soap/, templates/, scores/, privacy/, db/, frontend/, scripts/. plan.md의 전체 구조를 한 번에 만들지 않는다 ― 빈 디렉토리는 인지 부하만 늘린다. 구현 계획 1. 프로젝트 초기화 - uv init --python 3.11 (또는 수동으로 pyproject.toml 작성) - 의존성 추가:   - fastapi, uvicorn[standard], python-multipart (파일 업로드)   - mlx-whisper (PoC와 동일)   - numpy, scipy, sounddevice ― STT 엔진 자체는 wav 입력만 받으므로 sounddevice는 이번엔 불필요 (녹음은 클라이언트가 담당, 서버는 wav만 받음)   - dev: pytest, httpx (TestClient 용) - .python-version에 3.11 명시 - .gitignore: __pycache__/, .venv/, *.wav (recording.wav는 fixtures/에서만 추적), .DS_Store, uv.lock은 커밋 2. PoC → 모듈 이식 backend/stt/hints_loader.py (PoC load_hints() 이식): - 시그니처: load_hints(path: Path) -> str - 주석/빈 줄 제거 후 join - 파일 없으면 FileNotFoundError (PoC의 폴백 DEFAULT_PROMPT는 제거 ― 백엔드는 명시적 설정 강제) - 모듈 로딩 시 1회 호출 후 캐싱 (config에서 경로 받음) backend/stt/whisper_engine.py (PoC transcribe() 이식): - 시그니처: def transcribe_audio(     audio_path: Path,     model_repo: str,     prompt: str | None, ) -> TranscriptionResult - TranscriptionResult (pydantic):   - text: str   - elapsed_seconds: float   - audio_duration_seconds: float   - rtf: float   - low_confidence_segments: list[Segment] (avg_logprob < -0.5)   - model: str - PoC print_result의 신뢰도 낮은 구간 추출 로직을 결과 객체로 반환 (콘솔 출력 X) - get_audio_duration()도 같은 모듈에 (wav 헤더 파싱) 유지할 PoC 동작: - language="ko", word_timestamps=False, verbose=None - 동일한 mlx_whisper 호출 시그니처 → 회귀 동등성 보장 3. FastAPI 앱 backend/config.py: - Settings (pydantic-settings 또는 단순 dataclass):   - whisper_model_repo: str = "mlx-community/whisper-large-v3-mlx" (사용자 답변 반영 ― large-v3로 시작)   - hints_file: Path = Path("hints/medical_hints.txt")   - host: str = "127.0.0.1" (외부 접근 차단 ― plan.md 보안 원칙)   - port: int = 8080   - 환경변수로 override 가능 (WHISPER_MODEL_REPO 등) backend/main.py: - FastAPI 앱 생성, title="Voice SOAP Backend" - CORS는 활성화하지 않는다 (Phase 2 웹 UI에서 필요해질 때 추가) ― 지금 추가하면 보안 원칙 위반 + YAGNI - /healthz GET: {"status": "ok", "model": settings.whisper_model_repo} - api/transcribe.py의 라우터 include - 시작 시 힌트 사전 로딩(검증) ― 파일 없으면 startup에서 명확히 실패 backend/api/transcribe.py: - POST /transcribe:   - 입력: audio: UploadFile (multipart/form-data), 옵션 query use_hints: bool = True   - 처리: 임시 파일에 저장 → transcribe_audio() 호출 → 즉시 임시 파일 삭제 (plan.md 보안 원칙: 음성 보존 X)   - 출력: TranscriptionResult JSON   - wav 외 포맷은 일단 거부 (audio/wav만 허용) ― Phase 2에서 webm 등 추가 - 임시 파일은 tempfile.NamedTemporaryFile(delete=True, suffix=".wav") + try/finally로 보장 4. 보안 최소 조치 (이번 스프린트 분량) plan.md 7번 섹션 전체를 다 적용하진 않지만, API 표면이 뚫리지 않게 최소 조치는 한다: - host="127.0.0.1" 바인딩 (외부 차단) - 업로드 음성 파일은 처리 후 즉시 삭제 (디스크 잔존 X) - 로그에 텍스트 본문 출력 금지 (길이/메타데이터만) 마스킹/자동 cleanup/24h 정책은 SOAP 통합 + DB 도입 시 한꺼번에. 5. 테스트 tests/test_hints_loader.py: - 주석/빈 줄 제거 검증 - 파일 없을 때 FileNotFoundError tests/test_transcribe_api.py (옵션 ― 모델 로딩이 무거우므로 마커 분리): - @pytest.mark.integration ― recording.wav fixture로 실제 호출 - /healthz 200 OK는 단위 테스트 - transcribe 엔드포인트는 통합 테스트로 분리 (CI에선 skip, 로컬 수동 실행) 수동 회귀 검증 절차 (README에 명시): # 1. PoC 결과 기준선 python whisper_test.py --file recording.wav # 2. 백엔드 결과 uv run uvicorn backend.main:app --port 8080 & curl -X POST http://localhost:8080/transcribe \   -F "audio=@tests/fixtures/recording.wav" | jq .text # 3. 두 텍스트 동일 여부 육안 확인 비결정 사항 (다음 스프린트로 이월) - LLM SOAP 구조화 (/soap, /process) - LM Studio 클라이언트 + Gemma 4 MoE 프롬프트 ― 사용자 LLM은 준비됐으나 이번 스프린트 범위 밖 - 후처리 사전 (hints/postprocess.yaml) - LaunchAgent 등록 스크립트 - DB (SQLite/SQLModel) ― 메타데이터 저장이 필요해질 때 - 환자 식별자 마스킹 ― 텍스트가 LLM/DB로 들어가는 시점에 - 프론트엔드 ― Phase 2 검증 방법 (스프린트 종료 시) 1. healthcheck: curl http://localhost:8080/healthz → {"status":"ok","model":"..."} 2. OpenAPI: 브라우저에서 http://localhost:8080/docs 열어 transcribe 폼으로 wav 업로드 테스트 3. 회귀 동등성: 위 "수동 회귀 검증 절차"로 PoC 출력과 백엔드 출력 텍스트 일치 확인 4. 단위 테스트: uv run pytest tests/ -m "not integration" 통과 5. RTF 측정: 첫 호출(콜드) vs 두 번째 호출(웜) 시간 비교 → PoC와 동일하게 캐시 후 RTF 0.10x 근방인지 확인 6. 파일 잔존 확인: 업로드 후 ls /tmp/*.wav 또는 시스템 임시 디렉토리에 잔존 파일 없는지 확인 핵심 파일 / 참고 이식 원본 (PoC): - /Users/chlee/Developer/Claude_workplace_macmini/Voice_SOAP/whisper_test.py   - load_hints() (L23-41) → backend/stt/hints_loader.py   - transcribe() (L137-153) → backend/stt/whisper_engine.py   - get_audio_duration() (L156-161) → backend/stt/whisper_engine.py   - print_result()의 low confidence 추출 (L173-179) → 결과 객체 필드로 이동: - medical_hints.txt → hints/medical_hints.txt - recording.wav → tests/fixtures/recording.wav 참조 (수정 X): - plan.md ― 마스터 플랜, 변경 금지. 이번 스프린트는 11번 섹션의 항목 1~5 중 1, 3, 4 그리고 5의 STT 부분.